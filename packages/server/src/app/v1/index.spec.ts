describe('route', () => {
    it('should create render rout correct', () => {
        // expect(router.stack.some((s) => expect(s.route.stack).toEqual(
        //     expect.arrayContaining([
        //     expect.objectContaining({
        //         name: 'multerMiddleware'
        //     })
        // ]))))
        // expect(router.stack.some((s) => s.route.stack.length === 2)).toBeTruthy()
        // expect(router.stack.some((s) => Object.keys(s.route.methods).includes('post'))).toBeTruthy()
        // expect(router.stack.some((s) => s.route.path === ROUTE_RENDER)).toBeTruthy()

        expect(true).toBeTruthy();
    });
});
