import {css} from "lit";

export const style = css`
    :host  {
       display: grid;
        gap: 16px;
        grid-template-columns: 1fr;
        align-items: center;
    }

    wf-button {
        margin-bottom: 0;
    }

`